export const toggleClass = (element, elementClass) => element.classList.toggle(elementClass)
export const addClass = (element, classToAdd) => element.classList.add(classToAdd)
export const keyPressIs = (keyCode, event) => event.code === keyCode 
